Count Digit :
N = 12345
Output:5
pesudo code :
Brute Force Approach :
1. we need to extract the digit and keep the count variable outside
extract last digit while n>0
2.And then remove other digit in it
import java.util.*;
public class Main{
    public static void main(String[] args){
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
       int cnt = 0;
        while(n>0){
            cnt++;
            n/=10;
        }
        
        System.out.print(cnt);
    }
}

2.Using Function :
import java.util.*;
public class Main {
public static int countDigit(int num){
     int cnt = 0;
     while(num>0){
         cnt++;
         num/=10;
     }
     return cnt;
}

public static void main(String[] args){
    Scanner sc = new Scanner(System.in);
    int N = sc.nextInt();
    System.out.println("Number :" + N);
    int result = countDigit(N);
    System.out.print("Count of Digit :" + result);
}
}

Optimal Approach :
import java.util.*;
public class Main{
    public static int countDigit(int n){
        int cnt = (int)(Math.log10(n) + 1);
        return cnt;
        
    }
    public static void main(String[] args){
        Scanner sc = new Scanner(System.in);
        int N = sc.nextInt();
        int result = countDigit(N);
        System.out.print(result);
        
    }
}
--------------------------------------------------------------------------------------------------------------------------------------------------------------------
2. Reverse of an Number :
// Reverse a Number
// Input N = 12345
// Output = 54321

import java.util.*;
public class Main{
    public static void main(String[] args){
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int rev = 0;
        while(n>0){
            int ld=n%10;
            rev = (rev*10)+ld;
            n/=10;
        }
        
        System.out.print(rev);
        
    }
}

using Function :
import java.util.*;
public class Main{
    public static int reverseNum(int n){
        int rev = 0;
        while(n>0){
            int ld = n%10;
            rev = (rev*10) + ld;
            n/=10;
        }
        
        return rev;
        
    }
    public static void main(String[] args){
        Scanner sc = new Scanner(System.in);
        int N = sc.nextInt();
        int res = reverseNum(N);
        System.out.print(res);
        
    }
}


---------------------------------------------------------------------------------------------------------------------------------------------------------------------
1 . Amstrong Number :

import java.util.Scanner;

public class ArmstrongNumber {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input number
        System.out.print("Enter a number: ");
        int num = sc.nextInt();

        // Copy for checking later
        int original = num;
        int result = 0;

        // Count digits
        int n = String.valueOf(num).length();

        // Calculate sum of powers of digits
        while (num > 0) {
            int digit = num % 10;         // get last digit
            result += Math.pow(digit, n); // add digit^n
            num /= 10;                    // remove last digit
        }

        // Check Armstrong condition
        if (result == original) {
            System.out.println(original + " is an Armstrong number.");
        } else {
            System.out.println(original + " is NOT an Armstrong number.");
        }

        sc.close();
    }
}
Enter a number: 153
153 is an Armstrong number.

import java.util.*;
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int original = n;
        int ams = 0;
        int N = String.valueOf(n).length();
        
        while(n > 0) {
            int ld = n % 10;
            ams = ams + (int)Math.pow(ld, N);  // fixed
            n /= 10;
        }
        
        if(original == ams) {
            System.out.print("Armstrong Number");
        } else {
            System.out.print("Not an Armstrong");
        }
    }
}

Math.pow(ld, N) always returns a double, even if the inputs are integers.

But in your code, ams is declared as an int, so Java complains about “possible lossy conversion from double to int”.


2.Palindrome :
import java.util.Scanner;

public class PalindromeNumber {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input number
        System.out.print("Enter a number: ");
        int num = sc.nextInt();

        int original = num;
        int reversed = 0;

        // Reverse the number
        while (num > 0) {
            int digit = num % 10;          // extract last digit
            reversed = reversed * 10 + digit; // build reversed number
            num /= 10;                     // remove last digit
        }

        // Check palindrome condition
        if (original == reversed) {
            System.out.println(original + " is a Palindrome number.");
        } else {
            System.out.println(original + " is NOT a Palindrome number.");
        }

        sc.close();
    }
}

Enter a number: 121
121 is a Palindrome number.


3. Print all the divisors of number 
import java.util.Scanner;

public class Divisors {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input number
        System.out.print("Enter a number: ");
        int n = sc.nextInt();

        System.out.println("Divisors of " + n + " are:");

        // Loop through and check divisors
        for (int i = 1; i <= n; i++) {
            if (n % i == 0) {
                System.out.print(i + " ");
            }
        }

        sc.close();
    }
}

Enter a number: 28
Divisors of 28 are:
1 2 4 7 14 28
For i starts from => 1 to that n so for loop goes from 1 to 10
And checks for n%i ==0 => then it is a divisors .

import java.util.Scanner;
import java.util.TreeSet;

public class OptimizedDivisors {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input number
        System.out.print("Enter a number: ");
        int n = sc.nextInt();

        // Using TreeSet to keep divisors sorted automatically
        TreeSet<Integer> divisors = new TreeSet<>();

        // Loop only till sqrt(n)
        for (int i = 1; i * i <= n; i++) {
            if (n % i == 0) {
                divisors.add(i);        // add divisor
                divisors.add(n / i);    // add pair divisor
            }
        }

        System.out.println("Divisors of " + n + " are: " + divisors);

        sc.close();
    }
}
Enter a number: 28
Divisors of 28 are: [1, 2, 4, 7, 14, 28]

⚡Pro tip: If you need fast insertion + sorted + duplicate-free, use TreeSet.
If you need fast insertion + duplicates allowed, use ArrayList (but sort manually later).
--------------------------------------------------------------------------------------------------------------------------------------------------------------------

4. Prime Number

Optimal Approach :
A prime number is a number greater than 1 that has only two divisors: 1 and itself.

Examples:

Prime → 2, 3, 5, 7, 11, 13

Not Prime → 4 (divisible by 2), 9 (divisible by 3)
import java.util.Scanner;

public class PrimeNumber {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input number
        System.out.print("Enter a number: ");
        int n = sc.nextInt();

        boolean isPrime = true;

        // Prime check: number must be > 1
        if (n <= 1) {
            isPrime = false;
        } else {
            // Check divisibility from 2 to sqrt(n)
            for (int i = 2; i * i <= n; i++) {
                if (n % i == 0) {
                    isPrime = false;
                    break;
                }
            }
        }

        // Output result
        if (isPrime) {
            System.out.println(n + " is a Prime number.");
        } else {
            System.out.println(n + " is NOT a Prime number.");
        }

        sc.close();
    }
}

Brute Force Approach :
import java.util.Scanner;

public class PrimeBruteForce {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input number
        System.out.print("Enter a number: ");
        int n = sc.nextInt();

        boolean isPrime = true;

        // Prime must be greater than 1
        if (n <= 1) {
            isPrime = false;
        } else {
            // Check divisibility from 2 to n-1
            for (int i = 2; i < n; i++) {
                if (n % i == 0) {
                    isPrime = false;
                    break;
                }
            }
        }

        // Output result
        if (isPrime) {
            System.out.println(n + " is a Prime number.");
        } else {
            System.out.println(n + " is NOT a Prime number.");
        }

        sc.close();
    }
}

Time Complexity : O(root n)
1. O(log n)

Means the number of steps grows very slowly as n increases.

Usually comes from problems where input is divided in half each step.

Example: Binary Search

For n = 1,000,000 → only ~20 steps (because log₂(1,000,000) ≈ 20).

👉 Faster than O(√n) and O(n).

2. O(n log n)

Slightly slower than linear but still much better than quadratic.

Common in efficient sorting algorithms (Merge Sort, Quick Sort, Heap Sort).

Example: Sorting 1,000,000 numbers → ~20,000,000 operations (vs 1 trillion in O(n²)).

👉 Slower than O(n), faster than O(n²).
3. Relative Growth Comparison

Let’s rank them from best (fastest) to worst (slowest):

O(1) → Constant time (best)

O(log n) → Grows very slowly

O(√n) → Faster than linear but slower than log

O(n) → Linear growth

O(n log n) → Slightly worse than linear

O(n²) → Quadratic (bad for large n)

O(2ⁿ), O(n!) → Exponential/factorial (impossible for big n)

4. Analogy (to visualize 🚀)

Imagine you have to check 1,000,000 items:

O(1): Check directly → 1 step

O(log n): Halve each time → ~20 steps

O(√n): Walk √n → 1,000 steps

O(n): Check all → 1,000,000 steps

O(n log n): Sort them → 20,000,000 steps

O(n²): Compare everything with everything → 1,000,000,000,000 steps
-------------------------------------------------------------------------------------------------------------------------------------------------------------------

GCD :
The Greatest Common Divisor (GCD) of two numbers is the largest number that divides both of them completely.

Examples:

GCD(12, 18) = 6

GCD(14, 28) = 14

GCD(8, 9) = 1 (co-prime numbers)

Java Program – Brute Force Method

Check all numbers from 1 to min(a, b) and find the largest divisor.

import java.util.Scanner;

public class GCD_BruteForce {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input numbers
        System.out.print("Enter first number: ");
        int a = sc.nextInt();
        System.out.print("Enter second number: ");
        int b = sc.nextInt();

        int gcd = 1;
//We start with gcd = 1 because 1 is always a divisor of any number.
//This variable will store the largest number that divides both a and b.

        // Brute force: check all numbers up to min(a, b)
        for (int i = 1; i <= Math.min(a, b); i++) {
            if (a % i == 0 && b % i == 0) {
                gcd = i;
            }
        }

        System.out.println("GCD of " + a + " and " + b + " is: " + gcd);

        sc.close();
    }
}
Java Program – Euclidean Algorithm (Efficient)
import java.util.Scanner;

public class GCD_Euclidean {
    public static int gcd(int a, int b) {
        // Euclidean algorithm
        while (b != 0) {
            int temp = b;
            b = a % b;
            a = temp;
        }
        return a;
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Input numbers
        System.out.print("Enter first number: ");
        int a = sc.nextInt();
        System.out.print("Enter second number: ");
        int b = sc.nextInt();

        System.out.println("GCD of " + a + " and " + b + " is: " + gcd(a, b));

        sc.close();
    }
}
Enter first number: 48
Enter second number: 18
GCD of 48 and 18 is: 6

🔹 Solution With temp

That’s why we store the old b in temp before changing b:

int temp = b;   // save original b
b = a % b;      // calculate remainder
a = temp;       // now assign original b into a


This correctly implements the formula:

new a = old b

new b = old a % old b

🔹 Example (48, 18)

Step 1:

temp = 18

b = 48 % 18 = 12

a = 18

Now pair becomes (18, 12) ✅ which matches GCD(48, 18) = GCD(18, 12)

--------------------------------------------------------------------------------------------------------------------------------------------------------------------
Fibonacci :
import java.util.*;
public class Main{
    public static void main(String[] args){
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int fact=1;
        for(int i = 1;i<=n;i++){
            fact = fact*i;
        }
        System.out.print(fact);
    }
}
Large Values Problem

int can only hold up to about 2.1 billion (2^31-1).

Factorials grow very fast. Example: 13! = 6,227,020,800 (already bigger than int).

So for larger inputs, use long or BigInteger:

import java.math.BigInteger;
BigInteger fact = BigInteger.ONE;
for (int i = 1; i <= n; i++) {
    fact = fact.multiply(BigInteger.valueOf(i));
}


